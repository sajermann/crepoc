const URL = {
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
};

const DEFAULT_PAG = {
  pageIndex: 0,
  pageSize: 5,
  sort: 'updatedAt',
  desc: true,
};

export const CONSTANTS = {
  URL,
  DEFAULT_PAG,
};
